**Claude Code Prompt:**

Create a complete single-page web application for the product "NoteSync Pro" that meets the following specifications. The application should be entirely contained within a single HTML file named `index.html`, with no external dependencies. Follow the exact file structure, styling, and content guidelines provided below.

### File Structure:
- The entire application must reside in a single file named `index.html`.

### Color Scheme:
- Use the following colors consistently throughout the application:
  - Navy: `#0A0A1A`
  - Amber: `#F5A04A`

### Content Structure:
1. **Hero Section**:
   - Background color: Navy `#0A0A1A`
   - Content:
     - Product Name: "NoteSync Pro" (Large, bold font, white color)
     - Tagline: "Your notes, synchronized and accessible anytime, anywhere." (Medium font, amber color)

2. **Features Section**:
   - Background color: White
   - Display the top three features from the requirements in a grid layout:
     - **Feature 1**: Real-time data synchronization across devices.
     - **Feature 2**: Basic note-taking functionality with text formatting options.
     - **Feature 3**: User authentication and secure login with encryption.
   - Each feature should have an icon (use Font Awesome icons embedded in the HTML) and a short description.

3. **Call to Action Section**:
   - Background color: Amber `#F5A04A`
   - Content:
     - Heading: "Get Started with NoteSync Pro Today!" (Bold, white color)
     - Button: "Sign Up Now" (White text, navy background, rounded corners)

4. **Footer**:
   - Background color: Navy `#0A0A1A`
   - Content:
     - Text: "NoteSync Pro © 2023" (White color)

### Design Requirements:
- Ensure the design is fully responsive, adjusting layout for mobile, tablet, and desktop views.
- Use clean, modern CSS for styling, focusing on usability and aesthetics.
- Ensure all text is legible against background colors with appropriate contrast ratios.

### Additional Instructions:
- Embed any necessary CSS and JavaScript within the `<style>` and `<script>` tags in the HTML file, respectively.
- Ensure that the application runs smoothly with no external libraries or frameworks.

### Final Task:
Save everything to a single file called `index.html`.

---

By following this prompt, Claude Code should be able to construct a fully functional single-page web application for NoteSync Pro that adheres to all specified requirements, design guidelines, and structure without needing further clarification.